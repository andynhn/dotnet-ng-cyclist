import { FilterChatPipe } from './filter-chat.pipe';

describe('FilterChatPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterChatPipe();
    expect(pipe).toBeTruthy();
  });
});
